# Exclude node_modules (contains example credentials in library docs)
node_modules/

# Exclude previous scan results (they contain detected secrets)
trufflehog.json

# Exclude git objects (secrets in history are handled separately)
.git/
