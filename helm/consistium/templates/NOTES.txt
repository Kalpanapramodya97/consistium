◆ Consistium has been deployed successfully!

{{- if .Values.ingress.enabled }}

  Access the application via Ingress:
  {{- range $host := .Values.ingress.hosts }}
    {{- range .paths }}
    http{{ if $.Values.ingress.tls }}s{{ end }}://{{ $host.host }}{{ .path }}
    {{- end }}
  {{- end }}

{{- else if contains "NodePort" .Values.service.type }}

  Get the application URL by running:
  export NODE_PORT=$(kubectl get --namespace {{ .Release.Namespace }} -o jsonpath="{.spec.ports[0].nodePort}" services {{ include "consistium.fullname" . }})
  export NODE_IP=$(kubectl get nodes --namespace {{ .Release.Namespace }} -o jsonpath="{.items[0].status.addresses[0].address}")
  echo http://$NODE_IP:$NODE_PORT

{{- else if contains "LoadBalancer" .Values.service.type }}

  It may take a few minutes for the LoadBalancer IP to be available.
  Watch the status:
  kubectl get --namespace {{ .Release.Namespace }} svc -w {{ include "consistium.fullname" . }}

{{- else if contains "ClusterIP" .Values.service.type }}

  Port-forward to access the application locally:
  kubectl --namespace {{ .Release.Namespace }} port-forward svc/{{ include "consistium.fullname" . }} 3000:{{ .Values.service.port }}
  Then visit: http://localhost:3000

{{- end }}

  Useful commands:
  ─────────────────────────────────────────────────
  Check pod status:    kubectl get pods -l "app.kubernetes.io/name={{ include "consistium.name" . }}" -n {{ .Release.Namespace }}
  View logs:           kubectl logs -l "app.kubernetes.io/name={{ include "consistium.name" . }}" -n {{ .Release.Namespace }} -f
  Describe deployment: kubectl describe deployment {{ include "consistium.fullname" . }} -n {{ .Release.Namespace }}
  Check HPA:           kubectl get hpa {{ include "consistium.fullname" . }} -n {{ .Release.Namespace }}
  ─────────────────────────────────────────────────

  "You do not rise to the level of your goals. You fall to the level of your systems." — James Clear
